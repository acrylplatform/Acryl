${{header}}
${{detect-loader}}

if [ "$1" = purge ]; then
    rm -rf /var/lib/${{app_name}}/data
    rm -rf /var/log/${{app_name}}

    userdel acryl >/dev/null 2>&1 || true
    groupdel acryl >/dev/null 2>&1 || true
fi

exit 0
